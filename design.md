# Wen's Field Notes Design System

## 设计定位

这一版风格基于 `demo-v2-editorial`，关键词是：

- `Catppuccin Latte` 为默认明亮主题
- 保留 `Mocha` 作为深色模式映射
- 内容优先，不做 SaaS 仪表盘感
- 轻毛玻璃 + 干净白卡片，而不是大面积米黄色卡片
- 标题有编辑感，正文克制耐读
- 首页、`/me`、About 的职责必须清楚

## 视觉原则

1. 首页负责欢迎与导览，不承担复杂统计。
2. `/me` 是动态工作台，可以承接状态、指标、最近动态。
3. About 以纯文案为主，技术栈只做注脚，不做面板堆叠。
4. 内容列表页要强调“可扫描性”，而不是纯卡片堆叠。
5. 卡片不是默认容器，只有在信息需要被提起时才使用。

## 配色系统

### 默认主题：Latte

- 页面背景：`#eff1f5`
- 背景渐变终点：`#e6e9ef`
- 主要文字：`#4c4f69`
- 次级文字：`#6c6f85`
- 主强调色：`#8839ef`
- 主按钮/交互强调：`#7287fd`
- 博客标识：`#d20f39`
- 笔记标识：`#179299`
- 项目标识：`#40a02b`
- 标签标识：`#df8e1d`
- 卡片底色：`rgba(255,255,255,0.92)`
- 玻璃底色：`rgba(255,255,255,0.28)`
- 边框色：`rgba(108,111,133,0.16)`

### 深色模式：Mocha

- 页面背景：`#1e1e2e`
- 背景渐变终点：`#181825`
- 主要文字：`#cdd6f4`
- 次级文字：`#a6adc8`
- 主强调色：`#cba6f7`
- 主按钮/交互强调：`#89b4fa`
- 卡片底色：`rgba(49,50,68,0.86)`
- 玻璃底色：`rgba(49,50,68,0.28)`
- 边框色：`rgba(166,173,200,0.18)`

## 字体系统

### 标题字体

- 使用衬线标题栈：`"Iowan Old Style", "Palatino Linotype", Georgia, serif`
- 用于站点标题、页面 H1/H2、核心卡片标题

### 正文字体

- 使用：`"Aptos", "Segoe UI Variable Text", "Microsoft YaHei UI", sans-serif`
- 保持中文阅读稳定、现代、不过分技术化

### 层级建议

- H1：`clamp(2.7rem, 5vw, 5rem)`，`line-height: 0.96`
- H2：`clamp(1.9rem, 4vw, 3.2rem)`，`line-height: 1.02`
- H3：`1.25rem - 1.65rem`
- 正文：`1rem - 1.06rem`
- 元信息：`0.72rem - 0.74rem`，全大写或高字距小标题

## 间距系统

- 页面最大宽度：`1200px`
- 大区块垂直间距：`1.5rem - 4.5rem`
- 模块内边距：`1rem - 2rem`
- 卡片内边距：`1.1rem - 1.35rem`
- 卡片之间间距：`0.9rem - 1.5rem`
- 正文行高：`1.75 - 1.85`

规则：

- 大标题下必须留出足够呼吸感，不能贴近卡片群。
- 不允许把大标题和密集统计卡片硬塞进同一视觉层。
- 页面需要有“空白区”，不要所有空间都被卡片占满。

## 圆角与边框

- 超大容器：`30px`
- 标准卡片：`18px`
- 小卡片：`12px - 16px`
- 胶囊按钮/标签：`999px`

规则：

- 不要所有组件都用同一个圆角值。
- 同一屏里至少要有 2 到 3 种圆角层级，避免“模块长得一样”。
- 卡片边框优先使用极细描边，而不是重阴影。

## 阴影与毛玻璃

### 阴影

- 主阴影：`0 16px 45px rgba(156,160,176,0.18)`
- 轻阴影：`0 10px 28px rgba(156,160,176,0.12)`

### 毛玻璃使用范围

- 顶部导航
- 页面主容器 `section`
- 搜索栏
- 少量说明性信息卡

规则：

- 毛玻璃只作为气质增强，不能盖过内容本身。
- 正式内容卡片优先白色实体底，不要全部玻璃化。

## 组件规则

### 导航栏

- 吸顶
- 半透明玻璃底
- 链接使用轻胶囊 hover
- 品牌区简洁，不做厚重 logo 块

### 按钮

- 主按钮使用偏蓝紫强调
- 次按钮使用玻璃底或白底
- hover 只做轻微上浮，不做夸张动画

### 卡片

- 博客、笔记、项目必须通过徽章或边界差异区分
- 默认卡片白底
- 笔记类卡片可用更轻的左边强调线
- 项目卡片可允许更有节奏感的非对称圆角

### 标签/徽章

- 不使用高饱和实心紫块做全站通用标签
- 优先做“描边 + 淡底 + 分类色”的柔和徽章

### 搜索

- 主搜索框、类型过滤、标签过滤应该是一套组合工具带
- 不要让多个孤立表单控件抢主视觉

## 布局规则

### 首页

- 一屏只做欢迎语、简介、3 到 5 个入口
- 最近动态和内容预览可以往下放
- 不在首屏堆大量统计卡

### `/me`

- 第一优先级是“状态”和“最近在做什么”
- 指标区、动态区、重点项目区需要有明确主次
- 可以比首页密一点，但不能变成后台 dashboard

### About

- 以文案为主体
- 使用双栏或单栏长文排版
- 侧边只保留轻量注脚，不挂大卡片堆

### 列表页

- 让列表像“被编辑过的内容集合”
- 强化标题、摘要、meta 的扫描顺序
- 不同内容类型要有明显语义差异

## 实施建议

1. 先改全局 token、导航、容器、按钮、卡片。
2. 再改首页、`/me`、About、搜索的结构职责。
3. 最后统一博客、笔记、项目、标签、归档的细节样式。

## 禁止事项

- 不要回到暖黄米色大面积铺底
- 不要恢复背景网格纹理
- 不要所有模块都套同样的圆角白卡
- 不要让紫色徽章变成全局唯一强调方式
- 不要让首页、`/me`、About 再次讲重复的话
