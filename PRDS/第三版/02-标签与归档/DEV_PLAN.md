# 标签与归档开发计划

## 当前状态

状态：已在第一批正式实现中完成

## 开发目标

把第二版的单标签归档，升级成第三版正式的内容组织入口。

## 本轮目标

1. 新增 `/tags` 标签总览页
2. 新增 `/archive` 归档索引页
3. 统一标签统计与时间归档聚合结构
4. 打通与首页、个人主页、博客、笔记、搜索的导航关系

## 建议实施顺序

1. 先同步第三版总文档与模块文档状态
2. 抽出标签总览与归档索引的服务端聚合结构
3. 接入 `/tags` 与 `/archive` 路由和页面
4. 调整导航与内容入口
5. 验证页面输出与文档状态

## 完成标准

- `/tags` 可作为标签入口页正式访问
- `/archive` 可作为时间归档入口正式访问
- `/tags/:tag` 与总览、归档、搜索之间跳转自然
- 当前实现边界清楚，没有提前跨入数据库或后台能力

## 本轮完成情况

1. 已完成第三版总文档与模块文档同步
2. 已完成标签总览与归档索引的服务端聚合结构
3. 已完成 `/tags` 与 `/archive` 路由和页面
4. 已完成导航与内容入口调整
