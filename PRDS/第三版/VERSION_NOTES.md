# 第三版版本记录

## v3-complete

日期：2026-06-23

### 当前状态

- 第三版正式完成
- 个人主页、内容组织、内容关联、搜索升级、内容模型增强与内容治理都已落地

### 当前范围内已完成

1. `/me`
2. `/tags`
3. `/archive`
4. `/series/:slug`
5. 搜索筛选与结果升级
6. 三类详情页关联阅读
7. 内容校验入口

### 仍明确不做

- 数据库
- 后台系统
- 用户系统
- 持久化统计
- 外部数据同步

## v3-planning

日期：2026-06-22

### 当前状态

- 第三版尚未开始实现
- 当前已完成第三版总 PRDS 初稿

### 当前建议

- 第三版继续以个人主页、内容组织、关联、搜索与治理增强为主
- 不引入数据库、后台和用户系统

### 当前确定方向

1. 个人主页
2. 标签总览与归档增强
3. 内容关联模块
4. 搜索升级
5. 内容模型增强
6. 发布治理与校验

## v3-homepage-kickoff

日期：2026-06-22

### 当前状态

- 第三版阶段一已启动，个人主页进入正式实现
- 已确认第三版个人主页使用 `/me` 作为正式入口
- 已确认首页、`/me`、`/about` 需要同步调整职责分工

### 本轮目标

- 完成个人主页页面结构
- 接入内容入口、最近动态与轻量统计
- 打通与首页、`about`、`blog`、`notes`、`projects` 的导航关系
- 预留服务端主页聚合结构，为第四版后端能力演进留接口

### 本轮已完成

- 新增 `/me` 个人主页，并作为第三版正式入口接入主导航
- 首页改为第三版总览入口，about 收敛为背景与边界说明页
- 个人主页已接入内容入口、最近动态、重点项目、关注主题与轻量统计
- 服务端已新增主页聚合视图模型，统一装配 `blog / notes / projects` 内容
- sitemap 已包含 `/me`

### 本轮仍不做

- 数据库与持久化统计
- 后台管理系统
- 用户系统、任务系统、外部数据同步

## v3-index-kickoff

日期：2026-06-22

### 当前状态

- 第三版阶段二已启动，开始进入内容组织升级
- 已确认本轮先做 `/tags` 与 `/archive`
- 已确认项目内容暂不强行纳入时间归档主轴，避免提前触碰内容模型增强

### 本轮目标

- 完成标签总览页 `/tags`
- 完成归档索引页 `/archive`
- 统一标签统计与时间归档聚合结构
- 打通与首页、`/me`、`blog`、`notes`、`search` 的导航关系

### 本轮已完成

- 新增 `/tags` 标签总览页
- 新增 `/archive` 归档索引页
- 标签与时间归档已接入统一服务端聚合结构
- 首页、`/me`、标签详情页与搜索页已经补齐到 `/tags`、`/archive` 的跳转关系
- sitemap 已包含 `/tags` 与 `/archive`

### 本轮仍不做

- 数据库与持久化索引
- 访问量驱动的热门标签
- 项目内容的时间归档建模

## v3-finish-sprint

日期：2026-06-23

### 当前状态

- 第三版剩余阶段已启动，开始进入统一收尾实现
- 本轮会同时覆盖内容关联、搜索升级、内容模型增强、发布前治理

### 本轮目标

- 完成三类详情页的关联阅读
- 完成搜索筛选、排序与结果表达升级
- 完成 front matter 字段增强与系列页
- 完成内容校验脚本与发布前检查流程

### 本轮边界

- 仍不引入数据库、后台、持久化统计和外部同步
- 关联与搜索排序仍采用静态内容驱动逻辑

### 本轮已完成

- 三类详情页已接入关联阅读模块
- 搜索页已完成类型筛选、标签筛选、权重排序与命中解释升级
- `blog / notes / projects` front matter 已完成字段增强
- 已新增 `/series/:slug` 系列页
- 已新增内容校验二进制入口，覆盖 front matter、slug、坏链接和关键缺失项

### 本轮验证

- `cargo fmt --all`
- `cargo check`
- `cargo check -p server --bin validate_content`

### 本轮仍受环境影响

- `cargo run -p server --bin validate_content` 仍会受到当前机器 `msvc / gnullvm` 目标产物混用问题影响，暂时不能作为运行态验证
