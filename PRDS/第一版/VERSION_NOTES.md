# 第一版版本记录

## v0.1.0

日期：2026-06-21

### 本次完成

- 正式切换到 `Leptos + Leptos SSR + Markdown` 路线
- 建立 Rust workspace：`app`、`client`、`server`
- 打通 `Axum` + `Leptos SSR` 的基础运行链路
- 建立基于本地 `Markdown` 的博客内容读取与渲染
- 实现首页 `/`
- 实现博客列表页 `/blog`
- 实现博客详情页 `/blog/:slug`
- 实现关于页 `/about`
- 将首页和博客页细化为更接近 `static/demo-v1` 的正式组件结构
- 删除旧的 `frontend/` React/Vite 原型目录
- 删除历史残留空目录 `src/` 与 `templates/`

### 当前明确未做

- `notes` 正式页面
- `projects` 正式页面
- 数据库
- 评论系统
- 站内搜索
- 后台管理

### 当前项目判断

这次完成的是“第一版第一批正式实现范围”，不是“整个长期产品愿景已经全部完成”。

更准确地说：

- 博客主链路与正式项目骨架已经跑通
- 第一版里延后实现的内容仍然保留在规划中

### 建议的后续顺序

1. 正式接入 `notes`
2. 正式接入 `projects`
3. 补基础 SEO、RSS、sitemap
4. 继续完善文章导航、标签与内容组织体验

## v0.2.0

日期：2026-06-21

### 本次完成

- 正式实现笔记页 `/notes`
- 正式实现项目页 `/projects`
- 首页中的最近笔记区与代表项目区改为读取真实内容
- 博客详情页补齐上一篇 / 下一篇导航
- 接入基础 SEO 元信息
- 生成 `rss.xml`
- 生成 `sitemap.xml`
- 将 `01-首页`、`02-博客系统` 文档补齐到与当前代码一致

### 当前明确不做

- 数据库
- 评论系统
- 站内搜索
- 后台管理

### 当前项目判断

第一版的正式页面与基础站点输出能力已经补齐，当前进入“继续打磨与准备部署”的阶段。

### 建议的后续顺序

1. 继续打磨 `notes` 与 `projects` 的内容组织
2. 继续增强 SEO 细节
3. 准备部署文档与上线流程
